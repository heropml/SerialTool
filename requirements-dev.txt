# 开发 / 打包 / 单测依赖（含运行时，便于一条命令装齐）
-r requirements.txt

# 打包 exe / 重新生成图标 / 单测
pyinstaller>=6.0
Pillow>=10.0
pytest>=7.0
pytest-cov>=4.0
# 与 .pre-commit-config.yaml 的 ruff-pre-commit rev 对齐，避免本地/CI 漂移
ruff>=0.11.2,<0.12
pre-commit>=3.5
