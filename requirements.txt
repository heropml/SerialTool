# 运行时依赖
# PyQt5 自带 QtNetwork（TCP/UDP）；串口走 pyserial（统一版：串口 + 网络）
PyQt5>=5.15
pyserial>=3.5
# 波形图：pyqtgraph 实时绘曲线（依赖 numpy）
pyqtgraph>=0.13
numpy>=1.21

# 开发时依赖（打包 exe / 重新生成图标）
pyinstaller>=6.0
Pillow>=10.0
