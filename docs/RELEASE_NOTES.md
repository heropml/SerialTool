# NetworkTool v1.0.7

稳定性与代码审查修复版。网络调试核心功能（UDP / UDP 组播 / TCP Server / TCP Client）保持不变。

## 🛡️ 稳定性修复

- **TCP Client 连接超时保护** —— 连接不可达地址时不再干等系统默认 ~20 秒才报错，10 秒超时即主动中止并提示「连接超时」。
- **UDP 断开清理对端缓存** —— 关闭后清理最近对端地址，复用连接重开后不会再把「回复最近对端」发到上一会话的旧地址。

## 🔍 搜索功能修复

- **导航不再卡顿** —— 点 ▲/▼ 上下跳转改为 O(1) 着色，大数据量下不再每次全文重新扫描匹配。
- **打开搜索消除双重扫描** —— 选中文本打开查找栏（Ctrl+F）时不再重复执行两遍全文搜索。
- **实时数据搜索匹配跟随更新** —— 只用搜索、未配置关键字高亮规则时，持续接收的新数据中的匹配现在也会实时高亮、刷新「N/总数」计数并纳入 ▲/▼ 导航。
- 查找栏定位 viewport 守卫、计数标签守卫，规避极端时序下的异常。

## 🔧 其他

- **版本号解析** —— 正确处理预发布后缀（`1.0.7 > 1.0.7-rc1`），不再整段截断。

## 📦 下载

| 形式 | 文件 | 说明 |
|------|------|------|
| 安装版 | `NetworkTool_Setup_v1.0.7.exe` | 推荐，向导安装 + 桌面快捷方式 |
| 文件夹版 | `dist/NetworkTool/` | 解压即用，启动最快 |

> 无需安装 Python；Windows 10/11。在线更新「关于 → 检查更新」即可检测到 v1.0.7。
