## 人工验证请求 [HVR-S7-002]

**验证类型**: 行为回归 follow-up / 最终全量门禁
**所属步骤**: S7 循环审核与零回归缺陷闭环
**创建时间**: 2026-08-23 Asia/Shanghai
**硬件/串口**: 自动化不依赖实机

### 本轮修复

- [x] 打开打包示例保留原 `last_project_path`
- [x] 旧 HTTP / 局域网 Webhook 受信迁移，安全默认不放宽
- [x] 帧模板同名覆盖前确认
- [x] 首接收框 `textChanged` 单点绑定

### 自动验证结果

| 项目 | 结果 |
|---|---|
| Python | 3.11 全新 venv + `constraints-runtime.txt` |
| pytest | 1710 collected / 1702 passed / 8 skipped / 0 failed / 0 errors |
| Ruff / compileall | 通过 |
| 依赖 | `pip check` 无损坏依赖 |
| Bash / CI YAML | 语法通过 |
| diff check | 通过（仅 Git 提示 PowerShell 文件未来保持 CRLF） |

### 建议人工冒烟

1. 打开一个用户工程，再从快速开始进入示例并直接退出；重启应恢复原工程。
2. 在旧配置中触发一个 LAN Webhook，并在触发器编辑框确认“允许 HTTP/局域网”已显式勾选。
3. 将模板 A 另存为已有模板 B；取消时两条均保留，确认时才覆盖。

### 结论

```
自动化结论: 通过
剩余边界: 实机串口、真实 Webhook 服务、签名/公证仍需发布环境人工验证。
```
